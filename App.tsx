import { View } from 'react-native';
import MainNavigator from './src/navigator/MainNavigator';

const App = () => {
  return (
    <View style={{ flex: 1 }}>
      <MainNavigator />
    </View>
  );
};

export default App;
